<%= render "users/aaa/user_info" %>

<div class="container">
  <div class="row">
  
    <%= render "users/aaa/tabbar", selected_item: "好友清單" %>
    
    <div>
      <h3>等待回應中</h3>

      <ul>
        <% @wanted_friends.each do |girl| %>
          <li><%= girl.name %>&nbsp;<%= link_to '取消好友邀請', friendship_path(girl), method: :delete, class: "btn btn-danger btn-xs" %>
          </li>
        <% end %>
      </ul>
    </div>


    <div>
      <h3>想認識你的人</h3>

      <ul>
        <% @want2yous.each do |boy| %>
          <li>
            <%= boy.name %>
            <% if not current_user == boy %>
 
              <% if current_user.want_to_you?(boy) %>
                &nbsp;<%= link_to '拒絕好友邀請', reject_friendship_path(boy), method: :delete, class: "btn btn-danger btn-xs" %>
                <%= link_to '接受好友邀請', friendships_path(friend_id: boy), method: :post,class: "btn btn-warning btn-xs" %>
              <% end %>

            <% end %>
          </li>
        <% end %>
      </ul>
    </div>


    <div>
      <h3>你的好友</h3>

      <ul>
        <% @befrinds.each do |friend| %>
          <li>
          <%= friend.name %>&nbsp;<%= link_to '解除好友關係', friendship_path(friend), method: :delete, class: "btn btn-danger btn-xs" %>
          </li>
        <% end %>
      </ul>
    </div>  
    
    
  </div>
</div>
